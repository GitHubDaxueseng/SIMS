/*
 * package com.cam.dao;
 * 
 * import java.io.File; import java.net.URI; import java.net.URISyntaxException;
 * import java.util.ArrayList; import java.util.List;
 * 
 * public class ReadFile { public void ReadFile(String filePath) { File f =
 * null; f = new File(filePath); File[] files = f.listFiles(); // 得到f文件夹下面的所有文件。
 * List<File> list = new ArrayList<File>(); for (File file : files) {
 * list.add(file); } for(File file : files) {
 * System.out.println(file.getName()); } } }
 */